const Back = ({bible}) => {

    const handlePreviousButtonClick = () => {
        return;
    };

    return (<button onClick={() => handlePreviousButtonClick()}>Back</button>)
};

export default Back;